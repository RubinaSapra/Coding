vector<int> Solution::findPerm(const string A, int B) {
    vector<int> res;
    int st = 1;
    for(int i=0;i<A.length() ; i++)
    {
        if(A[i]=='I')
         res.push_back(st++);
         else
         res.push_back(st--);
    }
    res.push_back(st);
    return res;
}
