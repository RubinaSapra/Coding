void printKdistance(struct Node *root, int k)
{
  // Your code here
  if(!root) return;
  if(k==0)
  {
      cout<<root->data<<" ";
      return;
  }
  printKdistance(root->left, k-1);
  printKdistance(root->right, k-1);
}
